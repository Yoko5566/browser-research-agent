{
  "name": "browser_research_agent",
  "version": "1.0.0",
  "description": "Local Playwright workflow for reproducible public-page research captures.",
  "main": "scripts/capture_page.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.js",
    "capture": "node scripts/capture_page.js",
    "capture:batch": "node scripts/capture_batch.js",
    "playwright:install": "playwright install chromium"
  },
  "keywords": [
    "browser-research",
    "playwright",
    "web-capture",
    "markdown"
  ],
  "author": "Yoko",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
